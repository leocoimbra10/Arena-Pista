# IMPORTANTE: Configuração de Administradores

## ⚙️ Para adicionar um email como admin automático:

1. **Localmente (.env.local)**:
   - Crie um arquivo `.env.local` na pasta `app/`
   - Adicione a linha:
     ```
     VITE_ADMIN_EMAILS=seuemail@example.com,outroemail@example.com
     ```

2. **Na Vercel (Production)**:
   - Settings → Environment Variables
   - Adicione:
     - **Key:** `VITE_ADMIN_EMAILS`
     - **Value:** `seuemail@example.com,outroemail@example.com`
     - **Environment:** Production, Preview, Development

## 📧 Formato:

- **Múltiplos emails:** separe por vírgula
- **Case insensitive:** `USER@EMAIL.COM` = `user@email.com`
- **Sem espaços extras:** o código remove automaticamente

## ✅ Como Funciona:

1. Usuário se registra com um email da lista
2. Sistema automaticamente atribui `role: 'admin'`
3. Tab "Admin" aparece imediatamente após login
4. Não precisa configurar manualmente no Firestore!

## 🧪 Testando:

```bash
# No .env.local
VITE_ADMIN_EMAILS=marcioleocoimbra@gmail.com,vilaalecrimkids@gmail.com

# Agora crie uma conta com um desses emails
# → Você será admin automaticamente!
```

---

**Nota:** Emails já existentes no banco continuam com o role atual. Isso só afeta novos registros.
